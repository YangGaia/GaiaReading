# Gaia Reading 1.0.2

Gaia Reading 1.0.2 重点加入五种格式的书内全文搜索和 PDF 双页阅读，同时修复侧栏、窗口缩放与分页重排导致的裁切问题，并统一图书导入和书签操作。该版本继续提供 Windows x64 绿色免安装版，可以直接覆盖旧版本使用；原有书架、进度、书签、批注、阅读统计、AI 接口档案和设置会继续保留。

## 书内全文搜索

- EPUB、PDF、TXT、MOBI、AZW3 均可搜索全书文字，并显示章节、页码和命中摘要。
- 支持 `Ctrl + F` 打开搜索，使用 `Enter` / `Shift + Enter` 切换结果并定位高亮。
- 搜索会忽略大小写、无意义空白和全角/半角差异，并在本地缓存索引。
- 扫描版 PDF 没有文字层时会明确提示需要 OCR，不会返回误导性的空结果。

## PDF 双页与缩放

- PDF 新增单页/双页切换，双页支持 `1–2` 与 `2–3` 两种配对方式，适配封面、扉页和扫描页码错位。
- 每一页保留独立 Canvas 与文字层，搜索、划线、笔记、书签和 AI 上下文继续使用真实页码。
- 新增“适合页面”“适合宽度”和 10%～400% 手动缩放，最小缩放可完整显示单页。
- PDF 双页支持与其他格式相同的 0、24、56、96px 页面间隙。

## 阅读布局稳定性

- 修复搜索、书签、笔记或 AI 侧栏改变阅读区宽度后出现半页、裁切和关闭侧栏后版面不恢复的问题。
- 修复 EPUB 在搜索栏开启时切换窗口大小，正文仍保持旧窗口宽度的问题。
- 左缘目录感应在其他阅读工具开启时自动停用，避免打断搜索、书签、笔记和选区操作。
- 调整阅读顶栏中全文搜索与 AI 按钮的位置，使功能顺序更符合使用习惯。

## 图书导入与书签

- 首页和书架统一为“添加图书”，可选择文件夹、单个文件或一次多选文件。
- 文件夹继续递归扫描支持的电子书，重复图书会自动跳过并显示数量。
- “添加书签”恢复为与同级功能一致的中性样式；点击后立即打开书签侧栏并保存当前位置。

## 升级和运行方式

- 请先退出正在运行的旧版本，再启动下载的 `Gaia.Reading.1.0.2.exe`。
- 本版本为绿色免安装版，不需要安装 Node.js。
- Windows 首次运行未签名程序时可能显示 SmartScreen 提示；请确认文件来自本仓库 Release，并核对下方 SHA-256。

## 发布文件

- GitHub Release 文件：`Gaia.Reading.1.0.2.exe`
- 文件大小：135,285,046 字节
- SHA-256：`8B06A3CD09C84C4540A35F481710C0F04A93C30C756348DB526EEA54035E8B8C`

## 验证情况

- 完整自动化测试：206 项全部通过。
- Electron 基础启动、EPUB 真实导入与交互、PDF 双页与搜索冒烟测试通过。
- 便携版 exe 启动验证和 Git 差异格式检查通过。
