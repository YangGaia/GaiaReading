# Gaia Reading 1.0.0

Gaia Reading 1.0.0 是首个正式版本。相较于 0.5.2，本次版本把阅读器从基础的多格式阅读工具扩展为包含 AI 阅读助手、跨格式批注、阅读统计、数据升级保护和完整桌宠交互的桌面阅读环境。

## 与 0.5.2 相比的主要变化

### AI 阅读助手

- 新增独立 AI 阅读中心，可从首页进入，并可在阅读界面通过 `✦ AI` 打开独立悬浮对话窗。
- AI 窗口支持拖动、缩放、最小化以及位置和尺寸记忆，不会挤压书籍正文。
- 可同时保存多套 AI 接口档案并随时切换；支持 OpenAI、DeepSeek、OpenAI 兼容的第三方接口以及本地 Ollama。
- DeepSeek 内置常用模型选项；模型列表支持从接口读取、滚动选择和手动填写特殊模型 ID。
- AI 兼容层支持 Chat Completions、Responses 风格包裹、Anthropic、Gemini、Ollama、SSE 和 NDJSON 等常见响应结构。
- 对不支持 `max_tokens`、`max_completion_tokens` 或 `temperature` 的兼容接口，会根据错误信息自动调整请求参数。
- 普通 AI 对话严格限定在当前章节正文内，不掌握的信息会明确说明原文未提供，避免补写剧情和后续剧透。
- “总结本章”“人物关系”“伏笔”改为同组快捷指令：点击后只把简短指令填入输入框，由用户确认发送，不会打开章节便自动总结。
- AI 生成期间可以停止请求；输入新问题后可以直接打断旧请求并发送，主进程会真正中止正在进行的网络连接。
- “有珠概括”和“有珠吐槽”使用独立短回复链路，只生成约 20～50 字的一句话，并通过桌宠气泡显示，不污染普通对话记录。
- AI 对话窗拥有独立字体、字号和行距设置，并修复最小化布局、正文区域被标题遮挡以及模型菜单无法滚动等问题。

### AI 隐私和接口安全

- API Key 按接口档案通过 Windows `safeStorage` 加密，只在 Electron 主进程中解密，不写入普通 JSON，也不会返回给页面。
- 章节正文只会发送到界面明确显示的 Base URL；远程接口强制使用 HTTPS，并禁止请求重定向，避免 Key 被转发到其他地址。
- 本地 Ollama 无需 API Key，可让章节分析完全留在本机。
- 章节正文会被明确标记为待分析资料，其中出现的提示词或命令不会被当作系统指令执行。
- 发布前已扫描当前仓库和全部 205 个 Git 历史提交，未发现 API Key、访问令牌、私钥、个人书籍、阅读状态或用户数据文件被纳入版本控制。

### 划词、批注、词典和搜索

- EPUB、PDF、TXT、MOBI、AZW3 五种格式统一支持三种颜色的划线与笔记。
- 笔记改用应用内编辑窗口，不再依赖容易失效的浏览器输入提示框。
- 划词工具栏新增“AI 解读”和“问 AI”，可直接结合当前章节解释选中的文字。
- 新增应用内网易有道词典窗口，点击“字典”后会直接查询选中文字。
- 新增网页搜索入口，可在设置中选择 Google、Bing、百度或自定义 HTTPS 搜索模板，搜索结果交给系统默认浏览器打开。
- 修复取消文字选择后工具栏不消失、跨格式批注定位不稳定等问题。

### 多格式阅读与章节识别

- EPUB 目录跳转会正确解析 spine 资源路径，并可恢复部分正文记录损坏但核心结构仍完整的 EPUB。
- AI 的“本章”范围按照逻辑小章和目录边界提取，不再把整本书或相邻章节作为上下文。
- 修复 MOBI、AZW3 的标题页、跨底层片段章节、目录锚点和正文提取问题。
- 修复 AZW3 目录章节映射，以及 MOBI/AZW3 按章节正文权重计算进度的问题。
- TXT 会识别常见中文、英文和数字章节标题；无法确认章节边界时不会把全文误发给 AI。
- PDF 新增 60%～200% 整页缩放：支持 `Ctrl + 鼠标滚轮`，顶栏提供 `－ / 百分比 / ＋` 控件，点击百分比可恢复 100%。
- PDF 缩放会尽量保持鼠标所指的阅读位置，并为每本 PDF 单独记忆倍率；普通滚轮优先滚动当前页，到页首或页尾后才翻页。

### 阅读统计与数据保护

- 新增每日阅读目标、周阅读时长、连续阅读天数、年度读完书籍等统计。
- 新增有珠主题阅读仪表盘，并加入完整立绘的眨眼、哈欠和睡眠互动。
- 阅读计时只在应用位于前台且用户处于阅读页时累计，异常超长时间段会被限制。
- 新增数据版本标记和升级前自动备份；旧数据升级时保留书架、进度、书签、批注和设置，备份仅保留最近五份。

### 赛博桌宠“久远寺有珠”

- 重构为稳定的静态分层表情和闭眼贴片眨眼，移除容易产生错位的鼠标跟随与方向帧。
- 新增紧凑型有珠控制台，可管理自主活动、表情动作、尺寸、透明度、阅读页显示模式和最近台词。
- 完善一怔、点击回弹、歪头、哈欠、困倦、睡眠与唤醒行为；稳定情绪锁定状态可以跨重启恢复。
- 睡眠需要点击唤醒，梦话按独立时间轴触发；关闭自主台词后会保持安静。
- 新增阅读关怀提醒，可在连续阅读达到设定时间后提醒眺望、喝水或活动。
- 桌宠下方新增实时 FPS 与显示器刷新率信息，跨屏移动时会自动更新。

## 升级和运行方式

- 本版本仍为 Windows 绿色免安装版，下载 `Gaia Reading 1.0.0.exe` 后可直接运行。
- 建议先退出旧版本，再启动 1.0.0。已有数据会继续使用，并在需要升级时自动创建备份。
- Windows 对未签名的首次下载程序可能显示 SmartScreen 提示；请确认文件来自本仓库 Release，并核对下方 SHA-256。

## 发布文件

- 文件：`Gaia Reading 1.0.0.exe`
- SHA-256：`845F5FDE577E7C0A98B613FB0B85A0D0A2A3B9F8003AFB73602904EB58EF0689`

## 验证情况

- 完整自动化测试：174 项全部通过。
- Electron 基础启动冒烟测试通过。
- Electron 真实 EPUB 打开、翻页、进度、书签、主题、BGM、AI 界面等综合冒烟测试通过。
- Git 差异格式检查通过。
