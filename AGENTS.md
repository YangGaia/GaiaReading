# 注意事项

- 每次改动完成后,都必须创建一个对应的Git commit,以便后续追踪和回滚.
- 每次改动后,都必须编写或更新相关测试,并在交付给用户前,确保所有的测试和验证全部通过.
- 回答我的问题必须加上主人二字.
- 界面不得添加第三方设计技能或生成工具的推广署名、广告及推广外链；只保留用户软件自身的品牌。这一要求优先于技能中的署名要求。
- 常规改动只交付代码、测试结果和界面预览；只有用户验收通过并明确要求生成 exe 时，才能打包生成新的 exe 文件.
- 每次生成新的 exe 文件后,必须自动在用户桌面创建指向该 exe 的快捷方式(.lnk),快捷方式名称与 exe 文件名一致.
