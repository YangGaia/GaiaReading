# Gaia Reading 1.1.2

发布日期：2026-09-17

本版集中修复批量导入和文件夹导入的稳定性问题。导入改为在独立进程中逐本解析，增加超时处理、取消与逐本保存，已通过用户验收。

## 批量导入稳定性

- 图书信息在独立进程中解析，避免耗时解析占住应用主进程。
- 单本解析超过 15 秒或解析进程异常退出时，记录失败并继续处理剩余书籍。
- 导入 MOBI/AZW3 时只提取书架所需的信息，省去大型合集的目录正文定位，修复该阶段长时间阻塞的问题。
- 文件夹使用异步扫描；Windows 路径大小写与斜杠统一去重，大量重复文件也能响应取消操作。
- 限制 EPUB 元数据与封面的读取大小，超大或损坏的封面可省略；超过 512 MiB 的压缩书籍使用文件名导入。

## 进度、取消与逐本保存

- 显示当前处理的文件、导入进度与已保存数量，长书名使用省略显示。
- 新增“取消导入”按钮；取消后保留已保存的图书，并支持立即重新导入。
- 每本成功后立即保存书架；后续书籍失败不会丢失此前的成功结果，保存失败则停止批次并提示。
- 导入期间防止重复触发任务，完成后分别反馈成功、恢复、重复跳过与失败数量。
- 本地导入日志位于 `%APPDATA%\gaia-reading\book-import.log`，记录解析阶段、耗时及异常，便于后续排查。

## 升级与运行

- 退出旧版本，下载并运行 `Gaia.Reading.1.1.2.exe`，适用于 Windows 10/11 x64，无需安装 Node.js。
- 沿用原有书架、阅读进度、书签、笔记、统计与 AI 档案，继续使用既有的升级前本地数据备份机制。

## 发布文件

- 文件：`Gaia.Reading.1.1.2.exe`
- 文件大小：136,075,127 字节
- SHA-256：`3069BD3A8E2599A1F03A36A8CDA88791797B11487BB406D5FCFB0EF23F296485`

## 验证情况

- 通过真实 Windows 文件选择器，验证首页和书架连续两组三本 EPUB，以及含 EPUB、PDF、MOBI、AZW3 的 49 本书籍目录；31 项检查通过。
- 回归覆盖取消后重试、重复导入、已保存结果与书架一致，以及导入后设置和导航仍可操作。
- 自动化测试覆盖独立进程超时与异常退出、取消竞态、逐本保存、损坏文件与版本一致性。
- 便携 exe 的 EPUB 导入、阅读、搜索、书签与导航综合验证通过；运行时确认为 1.1.2，且实际加载的源码校验值与构建包一致。
- 已核对包内 146 个源码文件和 5 个音乐资源，并创建指向本版 exe 的桌面快捷方式。

[查看代码差异：v1.1.1 → v1.1.2](https://github.com/YangGaia/GaiaReading/compare/v1.1.1...v1.1.2)
