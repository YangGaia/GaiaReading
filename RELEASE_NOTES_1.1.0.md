# Gaia Reading 1.1.0

1.1.0 将首页、书架、阅读目标、AI 阅读中心和设置侧栏统一为石墨灰与银白细线设计，保留久远寺有珠的视觉主题，同时完善窗口适配、累计阅读计时和连续翻页体验。

## 2026-09-06 同版本更新

- 替换原 Release 的 `Gaia.Reading.1.1.0.exe`，版本号仍为 1.1.0；已下载旧文件的用户请重新下载。
- 修复首页、书架、阅读目标、AI 阅读中心之间，以及从书架进入阅读、退出阅读时的闪白；启动画面退场也保持连续背景。
- 页面使用短促的内容淡入与轻移过场，阅读主题在显示前应用；播放器和桌宠保持独立显示，连续翻页逻辑保持不变。
- 本次构建源码：[128e3ca](https://github.com/YangGaia/GaiaReading/commit/128e3cae98c60a9ba05d21ee423f7192b7429b63)。原 `v1.1.0` 标签保留，新增修复已同步到 `master`。

## 与 1.0.3 相比的主要变化

| 部分 | 1.1.0 的更新 |
| --- | --- |
| 首页 | 重新安排原有有珠立绘、字标与操作入口，加入边缘高光、阴影、悬停与按下反馈；缩放保持构图比例和界面清晰度。 |
| 书架 | 增加书籍边框、进度细节和悬停反馈，使用知更鸟与白色月牙点缀两侧，装饰与内容随窗口调整。 |
| 阅读目标 | 改为累计阅读计时器，保留表盘、数字读数、目标选择、每周统计、连续天数和年度已读书籍。 |
| AI 阅读中心 | 接口档案和表单统一材质与字体，支持窄窗口排列、键盘焦点和状态反馈，给桌宠预留空间。 |
| 设置与音乐 | 设置侧栏统一为深色材质；全软件音乐胶囊统一外观，非阅读页随窗口缩放，阅读页保持固定大小。 |
| 阅读配色 | 日间、夜间、护眼模式限定在阅读界面，其他页面保持统一设计。 |

## 累计阅读计时器

- 表盘顶部从 0 开始，每圈代表 60 分钟；分钟针指向今日累计分钟数，细秒针每圈代表 60 秒。
- 数字读数保留完整小时、分钟和秒，例如 1 小时 12 分钟显示为 `01:12:00`，分钟针指向 12 分钟刻度。
- 每日目标外圈独立表示完成度，仍可选择 15 / 30 / 45 / 60 分钟；停留在统计页面不会累计阅读时间。
- 每周阅读、连续天数、已读书籍与目标按钮增加细线、层次和交互反馈。

## 翻页与桌宠修复

- 修复翻页时闪白、出现空白帧以及文字拖影的问题，保留短促的逐页动画。
- 翻页输入不再等待动画结束，快速连按可以连续响应。
- 长按 `←` / `→` 进入高速连续翻页，保留动画；松开、改变方向或离开阅读时停止旧的连续操作，避免松手后继续补翻。
- 修复桌宠随首页一起放大、在窗口两侧遇到拖动限制，以及翻页时闪烁的问题；有珠继续使用独立尺寸、位置和控制台设置。

## GPT-6 模型 ID

- OpenAI 和自定义兼容接口均新增 **`gpt-6-astra`** 候选，选项直接显示模型 ID，并按该 ID 保存和发送请求。
- 原默认模型、已保存的接口档案和 Key 保持不变；自定义接口继续支持手动填写 ID、读取模型与候选去重。
- 模型 ID 依据 [OpenAI 官方 GPT-6 Astra 文档](https://developers.openai.com/api/docs/models/gpt-6-astra)。实际能否调用由账号权限和接口服务商支持情况决定。

## 升级和运行方式

- 退出正在运行的旧版本，下载并启动 `Gaia.Reading.1.1.0.exe`。
- 本版本为 Windows 10/11 x64 绿色免安装版，无需安装 Node.js。
- 沿用原有书架、阅读进度、书签、笔记、统计和 AI 档案；版本升级时继续使用既有的本地数据备份机制。
- EPUB、PDF、TXT、MOBI、AZW3 阅读、全文搜索、批注和 AI 对话能力继续保留。

## 发布文件

- 文件：`Gaia.Reading.1.1.0.exe`
- 文件大小：136,071,211 字节
- SHA-256：`FD8D9697CA94D8A803C9E50F47EFCA5A82E747EFA12BA29AF45681E1F9394E7D`

## 验证情况

- 完整自动化测试通过，包含版本一致性、README 截图与发布说明检查。
- 本版界面改动已通过 120 项 AI 页面实机检查和 439 项全量界面回归，覆盖窗口尺寸、阅读主题隔离、音乐缩放、桌宠与既有操作。
- 本次页面过场修复通过 1,328 项专项检查，覆盖 216 次切换、四种窗口尺寸、三种阅读主题与五种文件格式；全量界面回归再次通过。
- 发布前核验 exe 版本、打包资源、便携版启动和桌面快捷方式。
- AI 请求检查使用本机模拟服务，验证模型 ID 与兼容重试，不调用收费模型。

[查看完整代码差异：v1.0.3 → 本次 1.1.0 构建](https://github.com/YangGaia/GaiaReading/compare/v1.0.3...128e3cae98c60a9ba05d21ee423f7192b7429b63)
